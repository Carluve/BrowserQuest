# Dependencies
node_modules/

# Wrangler local state & temporary build artifacts (never commit / never deploy)
.wrangler/

# Local build output (regenerated by wrangler on deploy)
dist/

# Local dev vars / secrets (should they ever exist)
.dev.vars
.dev.vars.*
.env
.env.*
