{
  "name": "browserquest-cf",
  "version": "0.1.0",
  "private": true,
  "description": "BrowserQuest ported natively to Cloudflare Workers + Durable Objects",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "dependencies": {
    "underscore": "^1.13.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "playwright": "^1.61.1",
    "typescript": "^5.6.2",
    "wrangler": "^3.114.0"
  }
}
